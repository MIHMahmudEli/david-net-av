# Core (pipeline runs on these alone, using fallback encoders + dummy data)
torch>=2.1
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0

# Real backbones (uncomment when using videomae/wavlm)
# transformers>=4.40
# torchaudio>=2.1

# Preprocessing (src/data/preprocess.py). REQUIRED: ffmpeg + ffprobe on PATH.
# Face detection backend chain (best first; any one is enough, else center-fallback):
# insightface>=0.7 onnxruntime>=1.17   # RetinaFace + landmarks (preferred; true mouth crop)
# mediapipe>=0.10                      # fragile on ARM64/DGX Spark — see docs/07
# opencv-python-headless>=4.9          # Haar cascade fallback + faster image ops

# Data plumbing (optional)
# webdataset>=0.2

# Experiment tracking (optional)
# wandb>=0.16

# Serving
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
