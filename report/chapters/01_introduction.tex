% ================================================================ CHAPTER 1
% OBE 2.1 structure: 1.1 Background Analysis, 1.2 Existing Studies,
% 1.3 Research Motivation and Objective, 1.4 Research Contribution.
\chapter{Introduction}
\label{ch:introduction}

\section{Background Analysis}
\label{sec:background}

The rapid advancement of generative artificial intelligence has transformed the
creation of synthetic media from a specialized research activity into a
commodity capability. Face-swapping, facial reenactment, lip-synchronization
models, text-to-speech (TTS), and voice conversion (VC) systems are publicly
available, and their outputs are increasingly indistinguishable from genuine
recordings by human observers. International bodies and platform operators have
repeatedly warned that manipulated audio-visual content is now used for
disinformation, financial fraud, and non-consensual impersonation, making
reliable detection a societal necessity rather than a purely academic exercise.

The 2024--2026 period sharply escalated this threat along both modalities.
On the acoustic side, neural-codec language models such as
VALL-E~\cite{wang2023valle} demonstrated zero-shot voice cloning from a
three-second enrollment sample, and this capability was rapidly
productionized by factorized-codec and diffusion synthesizers such as
NaturalSpeech~3~\cite{ju2024naturalspeech3} and by openly released
multilingual systems such as XTTS~\cite{casanova2024xtts}, which place
high-fidelity voice
cloning in more than one hundred languages behind a single API call or a
consumer-grade GPU. On the visual side, audio-driven portrait animators such
as EMO~\cite{tian2024emo} and VASA-1~\cite{xu2024vasa} generate lip-synced,
emotionally expressive talking-head video of a target identity from a single
photograph---VASA-1 in real time. The
practical consequences are already documented: multimodal impersonation has
been used in corporate video-conference fraud with losses exceeding
US\$25~million in a single 2024 incident, in large-scale political
robocall and election-manipulation campaigns, and in pervasive
non-consensual intimate imagery. Regulators have responded---the European
Union AI Act~\cite{euaiact2024} imposes explicit transparency and labeling
obligations on synthetic media---but labeling presumes cooperative
generators; adversarial use requires \emph{detection}.

Measurement studies confirm that detection has not kept pace with
generation. The DeepfakeBench evaluation~\cite{yan2023deepfakebench} finds
that video detectors lose 10--25 AUC points as soon as the test corpus
changes, audio detectors trained on ASVspoof degrade sharply on speech
collected in the wild~\cite{muller2022inthewild}, and shortcut analyses show
that part of the reported in-domain accuracy is not forgery detection at
all~\cite{smeu2025shortcuts}. This gap between
laboratory accuracy and field robustness---driven by unseen generators,
re-compression, and mixed-modality manipulation---is precisely the failure
mode that motivates the generalization-first design of this thesis.

The root cause of the detection problem is that the visual and acoustic streams
of a video can be manipulated \emph{independently}. A single clip can therefore
fall into one of four authenticity quadrants, summarized in
Table~\ref{tab:quadrants}: both streams real (RVRA), a cloned voice dubbed over
real footage (RVFA), a swapped or reenacted face over genuine speech (FVRA), or
a fully synthetic combination (FVFA).

\begin{table}[htbp]
\centering
\caption{The four authenticity quadrants of a multimodal clip.}
\label{tab:quadrants}
\begin{tabular}{llll}
\toprule
\textbf{Quadrant} & \textbf{Video} & \textbf{Audio} & \textbf{Typical origin} \\
\midrule
RVRA & Real & Real & Genuine recording \\
RVFA & Real & \textbf{Fake} & Voice clone / TTS dubbed over real footage \\
FVRA & \textbf{Fake} & Real & Face swap / reenactment over real speech \\
FVFA & \textbf{Fake} & \textbf{Fake} & Fully synthetic avatar (lip-sync + TTS) \\
\bottomrule
\end{tabular}
\end{table}

This problem is important because the overwhelming majority of published
detectors collapse it into a single binary decision. Such a formulation is
forensically inadequate for three reasons. First, it provides no
\emph{attribution}: a journalist, court, or moderation system needs to know
whether the face was swapped, the voice was cloned, or both. Second, binary
detectors tend to shortcut on whichever modality is easier to classify,
degrading accuracy on the harder stream. Third, modern manipulations are
increasingly \emph{partial}---only a few seconds of audio or a short facial
segment are altered---which a clip-level binary label cannot express. Solving
per-modality attribution with temporal localization therefore places this
thesis in the correct context of the wider synthetic-media forensics problem
domain.

\section{Existing Studies}
\label{sec:existing}

Six research threads are directly relevant to this study. Comprehensive
surveys of the field are provided by Masood et al.~\cite{masood2023survey}
for generation and detection at large and Pei et al.~\cite{pei2024survey} for a
benchmark-oriented view that also covers the transition from single-modal to
multi-modal facial deepfake detection addressed by this thesis.

\textbf{Unimodal video detection.} Early detectors treated the task as
frame-level image classification: XceptionNet trained on
FaceForensics++~\cite{rossler2019faceforensics} became the canonical baseline,
followed by EfficientNet ensembles that won the Deepfake Detection
Challenge~\cite{dolhansky2020dfdc}. Temporal methods such as
LipForensics~\cite{haliassos2021lipforensics} and
RealForensics~\cite{haliassos2022realforensics} model mouth-motion dynamics for
stronger cross-manipulation generalization, and TALL~\cite{xu2023tall}
rearranges consecutive frames into thumbnail layouts so that a standard image
backbone captures temporal inconsistency. The most recent wave targets
\emph{generalization} explicitly: latent-space augmentation synthesizes
harder forgery variants in feature space~\cite{yan2024lsda}, LAA-Net combines
localized artifact attention with quality-agnostic training~\cite{nguyen2024laanet},
style-latent temporal flows expose the unnaturally stable style dynamics of
generated faces~\cite{choi2024stylelatent}, and Ba et
al.~\cite{ba2024exposing} extract complementary forgery clues through an
information-bottleneck objective. FreqBlender transfers the proxy-forgery idea
to the frequency domain by blending forgery-specific spectral components into
pristine faces~\cite{li2024freqblender}, natural-consistency
representations are learned from real videos alone~\cite{zhang2024naturalconsistency},
and facial-component-guided adaptation of a frozen foundation model
improves cross-dataset transfer~\cite{han2025facialcomponent}. The systematic DeepfakeBench
evaluation~\cite{yan2023deepfakebench} nevertheless shows that even these
detectors lose 10--25 AUC points under dataset shift---and all of them are
blind to audio manipulation.

\textbf{Unimodal audio spoofing detection.} The anti-spoofing community,
organized around the ASVspoof challenges~\cite{wang2020asvspoof}, progressed
from spectral features to end-to-end models such as
RawNet2~\cite{tak2021rawnet2} and the spectro-temporal graph attention network
AASIST~\cite{jung2022aasist}; self-supervised speech front-ends such as
wav2vec~2.0~\cite{tak2022ssl} and WavLM~\cite{chen2022wavlm} substantially
improved robustness. The 2024--2025 generation of resources reflects the
codec-based synthesis era: ASVspoof~5~\cite{wang2024asvspoof5} introduces
crowdsourced acoustic conditions and adversarial attacks at scale,
MLAAD~\cite{muller2024mlaad} covers 23 languages and dozens of TTS
architectures, Codecfake~\cite{xie2024codecfake} targets neural-codec
synthesizers directly, and SpoofCeleb~\cite{jung2025spoofceleb} sources
spoofing data from in-the-wild speech. On the modeling side,
style--linguistics mismatch~\cite{zhu2024slim} improves generalization to
unseen spoofing families, and Nes2Net shows that a lightweight nested head on
frozen foundation-model features is sufficient for state-of-the-art
anti-spoofing~\cite{liu2025nes2net}. At the same time, Shih et
al.~\cite{shih2024artifacts} show that such detectors lean on low-level
synthesis artifacts that do not survive re-encoding---a warning that shapes the
robustness protocol of this thesis. Audio-only methods, however, cannot detect
facial manipulation.

\textbf{Audio-visual binary detection.} Multimodal detectors fuse both streams
but almost universally emit one label: the Modality Dissonance
Score~\cite{chugh2020mds}, affective-cue matching~\cite{mittal2020emotions},
and transformer fusion models such as AVFakeNet~\cite{ilyas2023avfakenet} and
AVoiD-DF~\cite{yang2023avoiddf}, and ensembles of audio, visual and
audio-visual experts such as AVTENet~\cite{hashmi2025avtenet}. Temporal
variants exploit local audio-visual inconsistencies~\cite{astrid2025localtemporal}
or, for lip-syncing forgeries specifically, the temporal disagreement between
mouth motion and speech~\cite{liu2024lipslying}. The strongest recent representative,
AVFF~\cite{oorloff2024avff}, learns audio-visual correspondences with a
two-stage self-supervised objective before supervised fusion and reports
state-of-the-art accuracy on FakeAVCeleb---yet still collapses its output to
a single binary label. A real-video/fake-audio clip and a
fake-video/real-audio clip receive the same output, and per-modality
attribution is impossible by construction.

\textbf{Consistency and localization.} SyncNet~\cite{chung2016syncnet}-style
synchronization was exploited for anomaly-based detection~\cite{feng2023avanomaly},
but consistency methods detect \emph{mismatch} without attributing which stream
is fake and fail on fully synthetic clips whose fake streams are mutually
consistent. LAV-DF~\cite{cai2022lavdf} and
AV-Deepfake1M~\cite{cai2023avdeepfake1m} introduced content-driven temporal
forgery localization; BA-TFD+~\cite{cai2023batfdplus} and
UMMAFormer~\cite{zhang2023ummaformer} are the strongest published localizers,
contextual cross-modal attention~\cite{katamneni2024contextual} jointly scores
clips and frames; AV-Deepfake1M++~\cite{cai2025avdeepfake1mpp} adds real-world
perturbations to the localization benchmark.
Localization, however, is usually predicted \emph{jointly} over modalities
rather than per stream, and cross-dataset generalization of localizers is
under-reported.

\textbf{Proxy-forgery training.} Self-Blended Images~\cite{shiohara2022sbi} and
Face X-ray~\cite{li2020facexray} showed that synthesizing blending artifacts
from pristine images generalizes better than training on generator outputs; the
anti-spoofing literature found the same for vocoder
copy-synthesis~\cite{wang2023vocoded}, and latent-space
augmentation~\cite{yan2024lsda} extends the principle to feature space.
Zhou and Lim~\cite{zhou2021joint} explored joint audio-visual labels but
trained on generator outputs without a pretraining stage. No prior work
extends the proxy-forgery principle to the \emph{multimodal} setting in which
all four authenticity quadrants---and crucially a real-but-desynchronized
state---are manufactured from pristine data.

\textbf{Benchmarks and in-the-wild evaluation.} Standardized evaluation has
matured through DeepfakeBench~\cite{yan2023deepfakebench}, the 1M-Deepfakes
detection challenge~\cite{cai2024challenge}, and its perturbed successor
AV-Deepfake1M++~\cite{cai2025avdeepfake1mpp}. Smeu et
al.~\cite{smeu2025shortcuts} further show that audio-visual deepfake datasets,
FakeAVCeleb included, contain shortcuts (for example, leading silence and
duration statistics of the manipulated audio) that inflate in-domain
accuracy, and a reliability-oriented survey argues that calibration and
robustness must be reported alongside accuracy~\cite{wang2024reliabilitysurvey}.
Their shared conclusion is
that in-domain accuracy is effectively saturated while cross-generator and
in-the-wild performance remains the open problem---which is why the
evaluation protocol of this thesis treats cross-dataset and
leave-one-generator-out results as first-class evidence rather than
supplementary material.

\textbf{Research gap.} As summarized in Table~\ref{tab:gap}, no existing method
simultaneously provides (a) independent per-modality authenticity, (b)
cross-modal consistency reasoning, (c) temporal localization, and (d) validated
cross-generator generalization in a single reproducible, deployable system.
This study utilizes the strongest components of the existing literature
(self-supervised encoders, contrastive synchronization, proxy forgeries) and
extends them to close exactly this gap.

\begin{table}[htbp]
\centering
\caption{Capability comparison of prior work and the proposed framework.}
\label{tab:gap}
\small
\begin{tabular}{lcccc}
\toprule
\textbf{Approach} & \textbf{Per-modality} & \textbf{Consistency} & \textbf{Localization} & \textbf{Cross-gen.\ eval} \\
\midrule
Unimodal video~\cite{rossler2019faceforensics,haliassos2021lipforensics,nguyen2024laanet} & --      & --         & partial    & partial \\
Unimodal audio~\cite{jung2022aasist,zhu2024slim}       & --      & --         & partial    & partial \\
AV binary fusion~\cite{yang2023avoiddf,oorloff2024avff}     & --      & partial    & --         & partial \\
Sync/anomaly based~\cite{feng2023avanomaly}   & --      & \checkmark & partial    & partial \\
Localization~\cite{cai2023batfdplus,zhang2023ummaformer,katamneni2024contextual} & partial & partial   & \checkmark & -- \\
\textbf{Proposed framework} & \checkmark & \checkmark & \checkmark & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

\section{Research Motivation and Objective}
\label{sec:motivation}

The general aim of this thesis is to contribute a forensic framework that moves
synthetic-media detection from coarse binary classification to fine-grained,
per-modality attribution with temporal localization, and to demonstrate that
such attribution can generalize to generators never seen during training.

The specific objectives are:
\begin{itemize}
  \item to design a unified framework that detects, per modality, whether the
        video and the audio of a clip are real or AI-generated;
  \item to propose DAVID-Net, a disentangled cross-modal transformer with
        independent per-modality decision heads, a synchronization-consistency
        module, and per-modality temporal localization;
  \item to propose Quadrant-Aware Contrastive Pretraining (QACP), a
        generator-free pretraining strategy for cross-generator generalization;
  \item to evaluate the framework across multiple benchmarks with
        cross-dataset, robustness, calibration, and fairness protocols; and
  \item to deploy the detector as a publicly accessible web application.
\end{itemize}

The following research questions (RQs) are answered through the findings of
this research:
\begin{enumerate}[label=\textbf{RQ\arabic*.}]
  \item Can a single model produce independent, well-calibrated authenticity
        decisions for the visual and acoustic streams while remaining
        competitive with specialized unimodal detectors?
  \item Does explicit modeling of audio-visual synchronization improve
        detection of manipulations that break cross-modal coherence?
  \item Does disentangling modality-specific authenticity evidence from
        cross-modal consistency evidence improve generalization to unseen
        generators and datasets?
  \item Can the model temporally localize which segments of each stream were
        manipulated, and does this objective aid the clip-level decision?
  \item Is the detector robust to real-world degradations and fair across
        demographic subgroups?
  \item Can an embedding space pretrained on synthetically constructed
        quadrants built from pristine data alone generalize to real forgeries
        from unseen generators better than supervised training on generator
        outputs?
\end{enumerate}

\section{Research Contribution}
\label{sec:contribution}

The target users of the proposed solution are forensic analysts, journalists
and fact-checking organizations, online platform moderation systems, and
ordinary end users who need to verify received media. They benefit from
verdicts that state \emph{which} stream was manipulated and \emph{when}, with
calibrated confidence, rather than an unexplained binary flag; the deployed web
interface makes this capability directly accessible without technical
expertise.

The contribution of this thesis to the development of scientific concepts is:
\begin{itemize}
  \item \textbf{QACP}: a self-supervised pretraining stage that constructs all
        four authenticity quadrants from pristine data only and structures
        three embedding spaces with a factorized supervised-contrastive
        objective, including an explicit \emph{real-but-mismatched} class that
        teaches the model that desynchronization is not forgery;
  \item a reformulation of audio-visual deepfake detection as a multi-task,
        per-modality attribution and localization problem;
  \item \textbf{DAVID-Net}, a disentangled dual-branch cross-modal transformer
        architecture implementing this reformulation;
  \item a generalization-first evaluation protocol with leave-one-generator-out
        and cross-dataset testing as first-class results; and
  \item a complete, reproducible, publicly deployed detection system.
\end{itemize}

The remainder of this thesis is organized as follows.
Chapter~\ref{ch:methodology} presents the research methodology, including the
conceptual framework (DAVID-Net and QACP), the data collection process, ethical
considerations, and the economic analysis. Chapter~\ref{ch:results} reports the
results, their verification, and an impact analysis. The final chapter
concludes the thesis and outlines directions for future work.
